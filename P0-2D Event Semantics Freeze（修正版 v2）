# P0-2D Event Semantics Freeze（修正版 v2）

根据顾问审阅意见（commit `c03df0e7`），已对 Token 成本描述、Q2/Q5/Q6/Q9/Q12、Context Budget、5 AI 成本模型、Realism vs Token Cost 及"未实现/已实现"表述做了完整修订。

以下是**完整修正版文档**，可直接替换 GitHub 上的 `20260918_P0-2D_Event_Semantics_Freeze.md`。

---

```markdown
# 20260918_P0-2D_Event_Semantics_Freeze.md

> 版本：2026-09-18（v2 修正版）
> 校对依据：ext_ai.py / ext_world.py / ext_date.py / ext_econ.py / ext_shop.py / ext_instance.py / P0-2C Boundary Map
> 原则：只做语义冻结；不写运行时代码；不改现有行为
> 目标：定义"什么才算一个值得进入 V3.0 世界感知层的 Event"
>
> ⚠️ 本阶段**仅完成语义冻结**。文中所有"未来实现"的能力（EventBus / Scheduler / Wake / Brain / Decision / AI-AI 自主社交 / Context Budget 等）**均未在代码中实现**。

---

## 0. 本阶段边界

### 允许
- ✅ 建立 Event 语义定义表
- ✅ 明确 source / ai_name / payload 冻结语义
- ✅ 引入 LLM 触发级别（NONE / LOW / HIGH）
- ✅ 做 Token / 性能影响分析
- ✅ 完成"AI 真实生活目标保护检查"12 问
- ✅ 新增 1 个 .md 文档

### 禁止
- ❌ EventBus / EventStore / Scheduler / Wake / Brain / Decision / Action
- ❌ 修改任何 ext_*.py 行为
- ❌ 修改 main.data schema
- ❌ Memory 重构
- ❌ TTS / VoiceStudio 接入
- ❌ ai_id 迁移
- ❌ 前端修改
- ❌ 新增 API 端点

---

## 1. 核心定义（回炉）

### 1.1 Event 是什么

```
Event = 世界中确实发生了一件值得被 Agent 感知的事情。

Event 描述："刚刚发生了什么"（Fact）
Event 不描述："接下来该做什么"（Decision）
```

### 1.2 Event ≠ 函数调用

| 对象 | 为什么不是 Event |
|------|-----------------|
| `drive_ai()` | 函数入口，被多种触发调用 |
| `execute_action()` | 执行动作，不是动作本身 |
| `build_ai_context()` | 上下文构建 |
| `call_llm()` | LLM 调用 |
| `save_data()` | 持久化 |
| `_broadcast()` / `_hall_msg()` | 系统广播（不是 AI 主动发言） |
| `_group_talk()` | 派发器 |
| `auto_ai_loop()` | 30 秒循环 |
| `ai_shop_tick` / `ai_spot_tick` / `work_tick` / `date_tick` | 轮询循环 |

### 1.3 Event ≠ AgentState

| 维度 | AgentState | Event |
|------|-----------|-------|
| **时态** | 现在是什么状态 | 刚刚发生了什么 |
| **变更性** | 持续存在（投影） | 瞬时发生 |
| **来源** | main.data 只读投影 | 世界事实 |
| **示例** | "Dan 在咖啡馆，能量 70" | "Dan 进入了咖啡馆" |

### 1.4 Event ≠ Memory

| 维度 | Event | Memory |
|------|-------|--------|
| **时效** | 刚刚发生 | 过去累积 |
| **存储** | 运行时（P0-2C 阶段不持久化） | 持久化（旧 enqueue_event） |
| **消费者** | Brain（未来） | 夜间批处理 |
| **关系** | Event 可以**成为** Memory 的输入 | Memory ≠ Event |

### 1.5 Event ≠ 旧 `enqueue_event`

旧 `ext_memory.enqueue_event()` 是"给夜间 LLM 总结提供原料"的机制，共 16 处调用（见 P0-2C 附录 A）。**两者语义不同，未来可能映射，但 P0-2D 不做合并。**

---

## 2. Event 语义冻结表（逐项）

> 字段说明：
> - **source**：事件来源（user / ai / system / world / date / econ / shop / instance）
> - **ai_name**：事件主体（AI 名字），非 AI 事件为 None
> - **重复风险**：是否与其他 Event 重复表达同一世界变化
> - **LLM 级别**：NONE / LOW / HIGH（见 Section 5）

---

### 2.1 message_received ✅ 已完成（P0-2B）

| 字段 | 值 |
|------|-----|
| **世界事实** | 用户在某房间发送了一条消息 |
| **发生位置** | `ext_ai.wake_ais_for_room()` 内，群聊分支 `return` 之后 |
| **主体** | 用户（sender） |
| **source** | `"user"` |
| **ai_name** | `None`（P0-2B 已冻结） |
| **payload** | `{sender, room, content, target_ais}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ❌ 无（一条消息 = 一个 Event） |
| **LLM 级别** | **HIGH** |
| **理由** | 用户与 AI 的主要互动；AI 需要响应 |

---

### 2.2 message_sent 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 在某房间说出一句话（chat 单聊 / group 群聊） |
| **发生位置（2 处）** | ① `ext_ai.execute_action()` 的 `act == "speak"` 分支<br>② `ext_ai.drive_ai()` 的 `trigger == "group"` 分支 |
| **主体** | AI |
| **source** | `"ai"` |
| **ai_name** | **具体 AI 名字**（如 `"Dan"`），不是 source |
| **payload** | `{room, content, channel}`<br>· channel: `"chat"` / `"group"` |
| **Agent 应感知** | ✅ 是（其他 AI 可能感知同伴发言） |
| **重复风险** | ⚠️ 与 `message_received` 方向相反，不重复 |
| **LLM 级别** | **HIGH**（AI 主动输出，本身已是 LLM 结果） |
| **备注** | 系统广播（`_broadcast` / `_hall_msg` / `_broadcast_to_owner`）**不算** `message_sent` |

---

### 2.3 sms_sent 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 发出一条短信 |
| **发生位置** | `ext_ai.execute_action()` 的 `act == "sms"` 分支 |
| **主体** | AI |
| **source** | `"ai"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{to, content}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `message_sent` 语义不同（私信 vs 房间消息），**不应合并** |
| **LLM 级别** | **HIGH** |

---

### 2.4 relationship_changed 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 与主人的关系状态发生变化（affection 数值变化） |
| **发生位置（2 处）** | ① `ext_date._finish_date()`：`_set_aff(ai, aff)`（+5~9）<br>② `ext_date.date_accept_gift()`：`_set_aff(d['ai'], _aff(d['ai']) + 3)` |
| **主体** | AI |
| **source** | `"date"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{old_value, new_value, delta, reason}`<br>· reason: `"date_finished"` / `"gift_accepted"` |
| **Agent 应感知** | ⚠️ 低优先级（内部数值变化，非社交事件） |
| **重复风险** | ⚠️ 与 `date_finished` / `gift_received` 有重叠（数值变化是这两者的副作用） |
| **LLM 级别** | **NONE** |
| **处理建议** | 仅作为 Brain 决策的输入参数，不主动触发 LLM |

---

### 2.5 location_changed 🟡 冻结（⚠️ 核心语义问题，见 Section 3）

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 的 `ai_location` 发生改变 |
| **发生位置（15 处，见 P0-2C 2.1）** | —— |
| **主体** | AI |
| **source** | `"world"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{from_room, to_room, reason}` |
| **Agent 应感知** | ⚠️ 取决于 reason（见 Section 3） |
| **重复风险** | ⚠️ **高**——常与其他 Event 重复（如约会、工作） |
| **LLM 级别** | **NONE**（位置变化本身不值得 LLM） |
| **处理建议** | **仅当无更高层 Event 时才产生**（见 Section 3 规则） |

---

### 2.6 entered_location 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 进入了**一个新建筑**（跨越建筑边界） |
| **发生位置** | `ext_world._arrive()` 内部：`ai_spot_tick` 检测到 `st['last_bid'] != bid` 时触发 |
| **主体** | AI |
| **source** | `"world"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{building_id, building_name}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `location_changed` 语义重叠（见 Section 3） |
| **LLM 级别** | **LOW** |
| **处理建议** | 可进入 Agent 的低成本感知（如轨迹记录）；**不立即调用 LLM** |

---

### 2.7 work_started 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 开始了一份工作 |
| **发生位置（2 处）** | ① `ext_econ.auto_start_work()`（AI 自主/自动）<br>② `ext_econ.work_start()` API（用户手动） |
| **主体** | AI |
| **source** | `"econ"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{building_id, building_name, hours}` |
| **Agent 应感知** | ⚠️ 低优先级（自动行为） |
| **重复风险** | ❌ 无 |
| **LLM 级别** | **NONE**（自动行为，无需 LLM） |
| **备注** | **旧 `enqueue_event` 的真空区**（ext_econ 完全不调用） |

---

### 2.8 work_finished 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 完成工作，工资入账 |
| **发生位置（2 处）** | ① `ext_econ.pay_work()`（`work_tick` 定时结算）<br>② `ext_econ.work_stop()` API |
| **主体** | AI |
| **source** | `"econ"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{building_id, hours, earn}` |
| **Agent 应感知** | ⚠️ 低优先级 |
| **重复风险** | ❌ 无 |
| **LLM 级别** | **LOW** |
| **处理建议** | 可能触发 AI 的"想给主人发条短信"（下班分享），但**不直接触发 LLM**——由未来 Wake Score 决定 |

---

### 2.9 date_invited 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 某方向另一方发出了约会邀请 |
| **发生位置（2 个方向）** | ① 用户→AI：`ext_date.date_invite()` API<br>② AI→用户：`ext_date._trigger_invite()` |
| **主体** | 邀请方（用户或 AI） |
| **source** | `"date"`（不用 `"user"` / `"ai"`，因为两个方向共享语义） |
| **ai_name** | **被邀请的 AI**（两个方向都是这个 AI） |
| **payload** | `{inviter_type, inviter_name, building_id, building_name, channel}`<br>· inviter_type: `"user"` / `"ai"`<br>· channel: `"sms"` / `"chat"` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ❌ 无 |
| **LLM 级别** | **HIGH**（AI 需要响应——接受/婉拒/追问） |

---

### 2.10 date_agreed 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 约会邀请被接受 |
| **发生位置（2 处）** | ① `ext_date._accept_invite()`（用户邀约场景）<br>② `ext_date._process_reply()` 中 `result == 'accept'`（AI 邀约场景） |
| **主体** | AI（被邀请方） |
| **source** | `"date"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{user, building_id, room, arrive_min, invited_by_ai}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ❌ 无 |
| **LLM 级别** | **HIGH**（AI 需要表达开心/回应） |

---

### 2.11 date_started 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 双方到达约会地点，约会正式激活 |
| **发生位置** | `ext_date._arrive_date()`：`d['status'] = 'active'` |
| **主体** | AI |
| **source** | `"date"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{user, building_id, room}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `location_changed` 重叠（`_arrive_date` 也改 `ai_location`）；应由本 Event 吸收（见 Section 3） |
| **LLM 级别** | **HIGH**（见面场景） |

---

### 2.12 date_finished 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 约会结束 |
| **发生位置** | `ext_date._finish_date()`（用户结束/60 分钟自然结束/取消赴约） |
| **主体** | AI |
| **source** | `"date"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{user, building_id, room, duration_sec, affection_gain}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `relationship_changed` 重叠（affection 变化是副作用）；应由本 Event 吸收 |
| **LLM 级别** | **HIGH**（触发 AI 写随笔） |

---

### 2.13 gift_received 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 约会中主人收下 AI 送的礼物 |
| **发生位置** | `ext_date.date_accept_gift()` API |
| **主体** | AI（送礼方） |
| **source** | `"date"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{user, gift_id, gift_name, gift_icon}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `relationship_changed` 重叠（affection +3） |
| **LLM 级别** | **HIGH** |

---

### 2.14 item_bought 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 花钱买了一件东西 |
| **发生位置（2 处）** | ① `ext_shop.shop_buy()` API（**真人**购买）<br>② `ext_shop._buy_ai_item()`（**AI** 自主购买） |
| **主体** | 买家（人 / AI） |
| **source** | `"shop"` |
| **ai_name** | 买家是 AI 时填 AI 名字；买家是真人时 `None` |
| **payload** | `{buyer_type, buyer_name, item_id, item_name, qty, price, building_name}`<br>· buyer_type: `"user"` / `"ai"` |
| **Agent 应感知** | ⚠️ 低优先级（AI 购买时）；用户购买时不需要 AI 感知 |
| **重复风险** | ❌ 无 |
| **LLM 级别** | **NONE**（AI 自主购物）/ **LOW**（可能触发"给主人带一份"） |

---

### 2.15 gift_sent 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 送礼 |
| **发生位置（3 处）** | ① `ext_shop.shop_gift()` API（**真人送礼**）<br>② `ext_shop._do_reply_gift()`（**AI 回礼**）<br>③ `ext_shop._do_active_gift()`（**AI 主动送礼**） |
| **主体** | 送礼方（人 / AI） |
| **source** | `"shop"` |
| **ai_name** | 送礼方是 AI 时填 AI 名字；是真人时填**收礼的 AI 名字**（因为 AI 是被影响方） |
| **payload** | `{sender_type, sender_name, recipient, item_id, item_name, item_icon, gift_reason}`<br>· gift_reason: `"user_gift"` / `"ai_reply"` / `"ai_active"` |
| **Agent 应感知** | ✅ 是（AI 是送礼方或收礼方都需要） |
| **重复风险** | ❌ 无 |
| **LLM 级别** | **HIGH**（AI 送礼后可能触发 AI 表达/感谢；用户送礼时 AI 需响应） |

---

### 2.16 instance_created 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 副本被创建（草稿） |
| **发生位置** | `ext_instance.create_instance()` API |
| **主体** | 用户 |
| **source** | `"instance"` |
| **ai_name** | `None` |
| **payload** | `{user, iid, name}` |
| **Agent 应感知** | ⚠️ 低优先级（AI 尚未进入） |
| **重复风险** | ❌ 无 |
| **LLM 级别** | **NONE**（配置操作） |

---

### 2.17 instance_entered 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 副本被激活，AI 被锁定进入 |
| **发生位置** | `ext_instance.enter_instance()` API |
| **主体** | AI |
| **source** | `"instance"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{user, iid, name}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `location_changed` 重叠（进入副本设置 `ai_location = "_instance_{iid}"`）；应由本 Event 吸收 |
| **LLM 级别** | **HIGH**（进入副本是重要事件） |
| **备注** | 副本**内部**消息（`chat_history`）**不产生**主世界 Event |

---

### 2.18 instance_paused 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | AI 暂离副本 |
| **发生位置** | `ext_instance.pause_instance()` API |
| **主体** | AI |
| **source** | `"instance"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{user, iid, name, return_to_room}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `location_changed` 重叠（返回住宅） |
| **LLM 级别** | **HIGH** |

---

### 2.19 instance_finished 🟡 冻结

| 字段 | 值 |
|------|-----|
| **世界事实** | 副本结束，AI 解冻回住宅，生成总结 |
| **发生位置** | `ext_instance.end_instance()` API |
| **主体** | AI |
| **source** | `"instance"` |
| **ai_name** | 具体 AI 名字 |
| **payload** | `{user, iid, name, summary_preview, return_to_room}` |
| **Agent 应感知** | ✅ 是 |
| **重复风险** | ⚠️ 与 `location_changed` 重叠 |
| **LLM 级别** | **HIGH**（已调用 LLM 生成总结） |

---

## 3. location_changed 语义规则（⚠️ 核心问题）

### 3.1 问题陈述

`ai_location` 在 15 处被修改。如果每次赋值都产生 `location_changed` Event，会与 `date_started` / `work_started` / `instance_entered` 等**高层 Event 重复**。

例如：
- 约会到达时：`_arrive_date()` 同时改 `ai_location` 和 `status='active'`
- 如果两个 Event 都产生 → **语义重复**

### 3.2 冻结规则

**规则 L1**：`location_changed` **仅在没有更高层 Event 时产生。**

**规则 L2**：以下高层 Event **吸收** `location_changed`（不重复产生）：

| 高层 Event | 吸收的 location_changed 位置 |
|-----------|----------------------------|
| `date_started` | `_arrive_date()` |
| `work_started` | `auto_start_work()` / `work_start()` |
| `instance_entered` | `enter_instance()` |
| `instance_paused` | `pause_instance()` |
| `instance_finished` | `end_instance()` |
| `message_sent`（follow 分支） | `execute_action()` speak+follow 同建筑 |
| `message_sent`（普通分支） | `execute_action()` speak 普通 / note / diary |
| `entered_location` | `ext_world._arrive` 检测的跨建筑移动 |

**规则 L3**：以下情况**独立产生** `location_changed`：

| 位置 | reason | 说明 |
|------|--------|------|
| `execute_action()` **move** 分支 | `"self_move"` | AI 自主决定移动 |
| `_follow_arrive()` | `"follow_arrive"` | 跟随到达 |
| `drive_ai()` summon 分支 `_s()` | `"summon"` | 被召唤 |
| `_check_meetings()` | `"meeting_arrive"` | 约定到达 |
| `wake_ais_for_room()` 大喊分支 `arrive_and_reply()` | `"yell_arrive"` | 大喊到达 |

**规则 L4**：`_plan_auto` / `_home_activity` 触发的自主移动——**目前无独立位置修改代码**（它们通过 `drive_ai` → `execute_action` move 分支触发），复用 L3。

### 3.3 与 `entered_location` 的关系

**规则 L5**：`entered_location` 与 `location_changed` **不重复**。
- `entered_location` 只在**跨建筑**时触发（由 `ext_world._arrive` 检测）
- `location_changed` 在**同建筑内房间间移动**时触发

**规则 L6**：当跨建筑移动同时满足时，**只产生 `entered_location`**，不产生 `location_changed`。

### 3.4 冻结总结

```
location_changed 产生条件（全部满足）：
1. ai_location 真实变化
2. 该变化不是任何高层 Event 的副作用
3. 变化发生在同一建筑内部（跨建筑由 entered_location 吸收）

不产生的场景：
- 约会到达 → date_started 吸收
- 工作开始 → work_started 吸收
- 副本进入 → instance_entered 吸收
- 副本暂停 → instance_paused 吸收
- 副本结束 → instance_finished 吸收
```

---

## 4. source / ai_name 冻结语义

### 4.1 冻结规则

| 规则 | 内容 |
|------|------|
| **S1** | `source` **永远是类别**，不是具体名字 |
| **S2** | `ai_name` 是**事件主体**（AI 名字），非 AI 事件为 `None` |
| **S3** | 不要把 AI 名字放进 `source` |

### 4.2 冻结枚举

| source | 含义 | 典型 Event |
|--------|------|-----------|
| `"user"` | 用户触发的世界事实 | `message_received` |
| `"ai"` | AI 主动产生的世界事实 | `message_sent` / `sms_sent` |
| `"system"` | 系统行为 | （暂无 Event，保留） |
| `"world"` | 世界/位置/建筑相关 | `location_changed` / `entered_location` |
| `"date"` | 约会相关 | `date_*` / `gift_received` |
| `"econ"` | 经济/工作相关 | `work_started` / `work_finished` |
| `"shop"` | 购物/送礼相关 | `item_bought` / `gift_sent` |
| `"instance"` | 副本相关 | `instance_*` |

### 4.3 两个方向的 AI 事件

**AI 发送消息**：
- `source = "ai"`
- `ai_name = 具体 AI 名字`

**用户发送消息**：
- `source = "user"`
- `ai_name = None`（保持 P0-2B 语义）
- 具体目标 AI 放 `payload.target_ais`

---

## 5. LLM 触发级别（NONE / LOW / HIGH）

### 5.1 级别定义

| 级别 | 含义 | 未来处理机制（**尚未实现**） |
|------|------|------------------------|
| **NONE** | 世界发生了，但**不应该因此调用 LLM** | 仅更新 AgentState / 轨迹 / 记忆索引 |
| **LOW** | 可进入 Agent 的**低成本感知/摘要**机制 | 批量处理、延迟合并；**不立即调用 LLM** |
| **HIGH** | 有明显社交/生活/决策意义 | 可成为 Brain 的**候选触发源**（由 Wake Score 二次筛选） |

⚠️ **重要**：以上"未来处理机制"目前**仅为本阶段定义的原则**。`EventBus` / `Scheduler` / `Wake Score` / `Brain` / `Decision` **均未在代码中实现**。

### 5.2 冻结级别表

| Event | 级别 | 理由 |
|-------|------|------|
| `message_received` | **HIGH** | 用户与 AI 主要互动 |
| `message_sent` | **HIGH** | AI 主动输出 |
| `sms_sent` | **HIGH** | AI 主动私信 |
| `relationship_changed` | **NONE** | 内部数值变化，作为 Brain 输入 |
| `location_changed` | **NONE** | 位置变化本身不值得 LLM |
| `entered_location` | **LOW** | 可进入轨迹/记忆索引 |
| `work_started` | **NONE** | 自动行为 |
| `work_finished` | **LOW** | 可能触发"下班分享"（由未来 Wake Score 决定） |
| `date_invited` | **HIGH** | AI 需响应 |
| `date_agreed` | **HIGH** | AI 需表达 |
| `date_started` | **HIGH** | 见面场景 |
| `date_finished` | **HIGH** | 触发 AI 写随笔 |
| `gift_received` | **HIGH** | 情感场景 |
| `item_bought` | **NONE**（AI）/ **LOW**（用户侧） | 日常行为 |
| `gift_sent` | **HIGH** | 情感场景 |
| `instance_created` | **NONE** | 配置操作 |
| `instance_entered` | **HIGH** | 进入副本 |
| `instance_paused` | **HIGH** | 暂离副本 |
| `instance_finished` | **HIGH** | 副本结束 |

### 5.3 关键约束

**规则 LLM-1**：**NONE 级别 Event 永远不触发 LLM**。它们只更新：
- AgentState（未来）
- 轨迹（`add_trail` / `append_timeline`）
- 记忆索引

**规则 LLM-2**：LOW 级别 Event 进入**批量感知队列**（未来实现），聚合后由 Wake Score 决定是否触发。

**规则 LLM-3**：HIGH 级别 Event **仍由 Wake Score 二次筛选**——不是每个 HIGH 都调 LLM。

---

## 6. Token / Performance Impact 分析

### 6.1 当前 Token 成本基线（已确认事实）

**根据当前 Linkong 实际运行情况：**

```
用户主动聊天
    ↓
完整 AI 推理
    ↓
当前约 7,000–10,000 tokens
```

**原因**：当前 Prompt 上下文包含多个层次：

- 世界观
- + AI 人设
- + 用户画像
- + AI 当前状态
- + 关系信息
- + Memory / Impression
- + 聊天上下文
- + 其他系统上下文

⚠️ **重要澄清**：
- 这个数值是**当前一次完整 AI 对话的实际上下文成本基线**
- **不代表**每一个 Event 都应该消耗 7,000–10,000 tokens
- **不代表**所有 LLM 调用都必须达到这一规模
- 也不是未来优化后的最终成本

### 6.2 V3.0 真正要控制的成本

**V3.0 的目标不是"把所有 Prompt 都压到很小。"**

因为那可能损害我们原本非常重要的：
- 世界观
- AI 人设
- 用户画像
- 关系
- Memory
- 连续性

**我们的目标**：

> **让昂贵的完整 AI 思考只发生在真正值得思考的时候。**

### 6.3 V3.0 Token Cost Model（架构预留）

```
世界持续运行
    ↓
大量低成本世界事件
    ↓
Event
    ↓
NONE / LOW
    ↓
不调用完整 LLM

只有：
高价值事件
    ↓
Brain / Wake（未来实现）
    ↓
判断是否真的需要 AI 思考
    ↓
完整 LLM
    ↓
约 7,000–10,000 tokens（当前基线）
```

**真正需要控制的是**：

> **LLM 调用次数 × 单次上下文规模**

而**不是**单纯：
> Event 数量

### 6.4 具体示例

一个 AI 一小时发生：

```
location_changed × 10
item_bought × 2
work_started × 1
work_finished × 1
```

**不应该**意味着：
- ❌ 14 次完整 LLM
- ❌ 更不能意味着 14 × 7,000–10,000 tokens

**应该**：
- 这些日常事件尽可能通过**低成本机制**消化
- 只有少数真正重要的 Event 才走完整 LLM

### 6.5 问题 1：5 个 AI 同时生活时，哪些 Event 会高频发生？

| Event | 频率估算 | 说明 |
|-------|---------|------|
| `location_changed` | 每个 AI 每小时 1-3 次 | 自主移动 + 跟随 |
| `entered_location` | 每个 AI 每小时 0.5-1 次 | 跨建筑移动 |
| `message_sent` | 每次 AI 响应 1 次 | 用户对话频率相关 |
| `sms_sent` | 每个 AI 每天 0-5 次 | 主动关心 / 约会 |
| `item_bought` | 每个 AI 每天 1-3 次 | `ai_shop_tick` 每 60 秒检查 |
| `work_started/finished` | 每个 AI 每天 1-2 次 | 工作日 |
| `date_*` | 每个 AI 每天 0-1 次 | 受冷却限制 |
| `gift_sent` | 每个 AI 每天 0-2 次 | 回礼 / 主动送礼 |

**最高频**：`location_changed` > `message_sent` > `item_bought`

### 6.6 问题 2：哪些 Event 绝不能直接触发 LLM？

**绝对不触发**：
- `location_changed`（NONE）
- `work_started`（NONE）
- `item_bought`（NONE，AI 侧）
- `relationship_changed`（NONE）
- `instance_created`（NONE）

**理由**：这些是"日常世界事实"，每秒可能发生多次，触发 LLM 会导致：
- Token 成本爆炸
- AI 行为变得机械（"我一动位置就说话"）

### 6.7 问题 3：哪些 Event 可以批量、合并、延迟处理？

| Event | 合并策略（未来实现） |
|-------|---------------------|
| `location_changed` | 合并为"过去 30 分钟的移动摘要" |
| `entered_location` | 合并为"最近访问的建筑" |
| `item_bought` | 合并为"今日消费摘要" |
| `work_started/finished` | 合并为"今日工作" |
| `relationship_changed` | 合并为"关系变化累积" |

**触发时机**：由未来 Wake Score 决定。

### 6.8 问题 4：如何避免"高频 LLM"？

**四层防御（未来实现）**：

```
第一层：级别筛选
  只有 HIGH 才进入 Brain 候选

第二层：Wake Score 筛选
  wake_score = event_importance + social_need + goal_urgency
             + emotional_arousal + relationship_trigger
             + time_trigger + memory_cue
  只有 wake_score > 阈值 才真正调用

第三层：冷却机制
  每个 AI 有 LLM 调用冷却（如 30 秒）
  同一 AI 短时间内多次 HIGH Event 合并

第四层：批量处理
  低优先级 Event 进入队列，由 Scheduler 周期处理
```

⚠️ 以上四层目前**均为架构原则**，**尚未在代码中实现**。

### 6.9 问题 5：未来 Brain 应采用什么原则？

**核心原则**：**"AI 不是每秒都思考"**

- **事件驱动**，不是轮询
- **Wake Score 决定**，不是 Event 级别直接决定
- **批量处理** 低优先级
- **冷却机制** 保护高频

**未来目标**（待真实运行数据建立，见 6.10）：通过真实运行数据决定合理预算，而非硬编码。

### 6.10 5 AI 成本模型（不预先冻结）

**当前不能预先冻结每个 AI 每小时允许多少次 LLM。**

原因：实际频率取决于：
- 用户聊天量
- AI 自主行为
- AI-AI 社交
- Event 频率
- Wake 策略
- Context 大小
- 模型价格
- 用户体验要求

**未来应该通过真实运行数据建立**：

| 指标 | 说明 |
|------|------|
| Per AI LLM calls/hour | 每个 AI 每小时 LLM 调用次数 |
| Tokens/hour | 每小时 token 消耗 |
| Tokens/day | 每天 token 消耗 |
| Average context tokens | 平均上下文规模 |
| Average output tokens | 平均输出规模 |
| Event → LLM conversion rate | Event 转 LLM 的比率 |

**然后再决定合理预算。**

### 6.11 Realism vs Token Cost

**真人感 ≠ 每件事情都让 AI 思考。**

真人生活本身也是：
- 大量自动行为
- + 少量真正思考
- + 记忆
- + 习惯
- + 关系
- + 偶发重要决定

**V3.0 的方向**：

| 上升 ⬆️ | 下降 ⬇️ |
|---------|---------|
| 世界运行频率 | 无意义 LLM 调用 |
| AI 感知能力 | 重复 Prompt |
| AI 自主性 | 完整历史注入 |
| AI-AI 社交 | 无意义 Event → LLM |
| 关系连续性 | —— |

**最终目标**：

> **让 AI 看起来更像真人，而不是让服务器替 AI 每秒思考一次。**

---

## 7. AI 真实生活目标保护检查（12 问）

### Q1：是否支持最多 5 个 AI 在同一共享世界同时生活？

✅ **当前支持**。P0-2C 的 Event 设计基于 `main.data` 共享状态，`ai_location` / `messages` / `work_sessions` 等字段天然支持多 AI 并行。

### Q2：是否支持未来 AI ↔ AI 自主互动？

⚠️ **部分存在**。需要更准确的表述：

| 维度 | 状态 |
|------|------|
| 多 AI 共享世界 | ✅ 已具备 |
| 群聊同时响应 | ✅ 已具备 |
| 真正独立的 AI ↔ AI 自主社交生命周期 | ❌ **尚未形成** |
| V3.0 目标 | 建立真正的 AI ↔ AI 自主互动 |
| P0-2D 状态 | 仅完成 Event 语义层预留 |

**特别注意**：不能因为 `message_sent` 可以标识 AI，就说 AI-AI 自主互动已经被架构解决。

我们最终真正需要的是：

```
AI A
    ↓
主动产生社交意图
    ↓
联系 AI B
    ↓
AI B 感知
    ↓
AI B 判断
    ↓
AI B 决定是否回应
    ↓
关系变化
    ↓
新的 Event
```

这才是我们最终要做的 AI 社区。

### Q3：是否支持未来 AI 自主发起行为？

⚠️ **部分存在**（**已定义原则，未来扩展**）：
- 已有 `auto_ai_loop` + `_plan_auto`（AI 自主生活）✅ 当前存在
- 已有 `_ai_think_invite`（AI 自主约会）✅ 当前存在
- 未来可扩展更多自主行为（如 AI 间社交）❌ 未实现

### Q4：是否为 Brain / Decision 留出清晰接口？

✅ **架构预留**（**未实现**）：
- Event 携带 `source` / `ai_name` / `payload`（P0-2A 已定义）
- Event 携带 `LLM 级别` 隐含字段（P0-2D 冻结）
- 未来可添加 `importance` / `emotional` / `wake_score` 字段（未实现）

### Q5：是否能让世界持续运行，同时避免高频 LLM？

⚠️ **P0-2D 已定义未来应该采用的低成本原则。当前实现尚未建立完整的 Wake / Brain / Scheduler。**

| 层面 | 状态 |
|------|------|
| LLM 级别 NONE / LOW / HIGH 定义 | ✅ P0-2D 已冻结 |
| Wake Score 概念 | 🟡 架构预留 |
| 冷却机制 | ❌ 未实现 |
| 事件聚合 | ❌ 未实现 |
| Scheduler | ❌ 未实现 |

**未来目标**：通过这些机制控制 LLM 调用频率。

### Q6：是否能做到只有高价值事件进入未来 Brain / LLM？

⚠️ **P0-2D 已定义原则，当前尚未实现。**

| 层面 | 状态 |
|------|------|
| HIGH 级别 Event 冻结 | ✅ P0-2D 已定义 |
| Wake Score 二次筛选 | ❌ 未实现 |
| Brain / Decision | ❌ 未实现 |

**当前不能声称"已经实现"**——只能说"已完成语义设计，未来实现"。

### Q7：是否保留现有能力（聊天/自主生活/工作/约会/购物/记忆/副本）？

✅ **完全保留**：
- 本阶段未修改任何 ext_*.py
- 所有现有功能正常运行
- Event 层是**旁路观察**，不影响主流程

### Q8：未来是否可以接入 VoiceStudio / TTS 而不破坏 Event / Brain / Action 架构？

✅ **可以**（**架构预留，未实现**）：
- VoiceStudio / TTS 是**输出层**，不参与 Event / Brain 逻辑
- 现有 `sms_sent` / `message_sent` Event 可作为 TTS 触发源
- 只需在 Action 层增加 TTS 调用，不触碰 Event / Brain

### Q9：是否可以避免把无限增长的聊天历史直接塞进 Prompt？

⚠️ **当前存在部分限制，但尚未完成长期解决。**

**当前措施（缓解，不是最终架构）**：
- 最近消息/时间线截断（`build_ai_context` 已截取最近 20 条时间线 + 最近对话）
- 后端消息截断 300 条
- 前端缓存 400 条
- 分片渲染

**我们真正担心的**：

```
聊天数据越来越多
    ↓
后端数据越来越大
    ↓
读取 / 序列化 / 传输越来越多
    ↓
页面打开越来越慢

以及：

历史越来越多
    ↓
为了保持 AI 连续性
    ↓
Prompt 上下文越来越复杂
    ↓
7000–10000 tokens 基线继续上涨
```

**未来需要独立解决（P1 阶段）**：
- Chat Pagination
- Recent Context
- Conversation Summary
- Long-term Memory
- On-demand History Retrieval
- Context Budget

### Q10：是否为副本 / 多元宇宙留出清晰数据边界？

✅ **是**（**当前已实现**）：
- 副本有独立 `chat_history`（与主世界 `messages` 隔离）
- 副本有独立 `background` / `premise` / `time_setting`
- 副本内消息**不产生主世界 Event**
- 未来可扩展 `world_id` / `instance_id` 字段（架构预留）

### Q11：5 个 AI 长期运行时，是否存在 Token 成本失控风险？

⚠️ **有风险，P0-2D 已定义应对原则，但尚未实现机制。**

**已冻结的原则**：
- NONE 级别 Event 不调用 LLM
- LOW 级别批量处理（未来实现）
- HIGH 级别由 Wake Score 二次筛选（未来实现）
- **未来 Scheduler 必须实现冷却机制**（未实现）

**P0-2D 结论**：**架构层面已定义预防原则**，实现层面待 P0-3+。

**具体预算值不预先冻结**（见 6.10），需真实运行数据建立。

### Q12：随着聊天数据越来越多，是否存在页面打开越来越慢的问题？

⚠️ **已确认存在长期性能风险，并且当前已经出现聊天页面加载变慢现象。**

**这是一个已经存在的产品问题，不是未来可能发生的问题。**

**P0-2D 不处理实现。**

**后续必须建立独立的**：

> **P1 — Chat Performance & Memory Architecture**

重点解决：

1. 页面打开不读取完整历史
2. 首屏只读取最近消息
3. 历史消息分页
4. 历史按需加载
5. 前后端避免重复传输大量历史
6. 聊天摘要与长期 Memory 分离
7. AI Prompt 不直接依赖完整聊天历史
8. 数据增长不应线性拖慢页面首屏

**核心目标**：

> **"数据可以无限增长，但首屏加载成本不能跟着历史数据线性增长。"**

---

## 8. Future Context Budget（未来上下文预算）

这是未来架构边界，**P0-2D 只记录原则，不实现**。

我们不是简单追求：
> Prompt 越小越好

而是未来应该形成：

```
固定预算 + 分层上下文 + 按需检索
```

**示例分层**：

| 层级 | 内容 | 类型 |
|------|------|------|
| **永久核心** | 世界观 / 人设 / 用户核心画像 | 固定 |
| **稳定上下文** | 近期聊天 / 当前事件 | 短期 |
| **长期** | Memory / Relationship / History | 按需检索 |
| **世界** | 当前 AgentState / 当前 Event | 动态 |

**最终目标**：

```
历史数据 ↑↑↑
但
单次 AI 推理上下文
不应该无限 ↑
```

**这件事情以后由 P1 Memory / Context Architecture 具体设计。**

---

## 9. 特别保护：产品目标

### 9.1 不是要变成

❌ "一个会自动聊天的网页"
❌ "一个让 AI 每 30 秒思考一次的烧钱机器"
❌ "一堆功能堆叠的集合"

### 9.2 而是要成为

✅ **临空市：一个会自己呼吸的 AI 社区**

```
世界发生事件
    ↓
Event
    ↓
Agent 感知
    ↓
AgentState + Memory + Goal
    ↓
Brain
    ↓
Decision
    ↓
现有 Linkong Capability
    ↓
世界发生变化
    ↓
新的 Event ↺
```

### 9.3 核心方向

- 生活 → 生态 → 世界 → 多元宇宙
- 用户参与 + AI 自主 = 共同生活
- 低 Token 成本 + 高智能感

---

## 10. 现有系统保护清单

### 10.1 P0-2D 禁止修改

| 系统 | 位置 | 状态 |
|------|------|------|
| `ext_ai.py` | 全部 | ❌ 不修改 |
| `ext_world.py` | 全部 | ❌ 不修改 |
| `ext_date.py` | 全部 | ❌ 不修改 |
| `ext_econ.py` | 全部 | ❌ 不修改 |
| `ext_shop.py` | 全部 | ❌ 不修改 |
| `ext_instance.py` | 全部 | ❌ 不修改 |
| `ext_memory.py` | 全部 | ❌ 不修改 |
| `main.py` | 全部 | ❌ 不修改 |
| `agent/state.py` | 全部 | ❌ 不修改 |
| `agent/event.py` | 全部 | ❌ 不修改 |
| `agent/event_adapter.py` | 全部 | ❌ 不修改 |
| 前端任何文件 | 全部 | ❌ 不修改 |
| `data.json` schema | 全部 | ❌ 不修改 |

### 10.2 特别禁止

- ❌ EventBus / EventStore / EventQueue
- ❌ Scheduler / Wake / Brain / Decision Engine / Action Engine
- ❌ Memory 重构
- ❌ TTS / VoiceStudio 接入
- ❌ ai_id 迁移
- ❌ 重写 ext_ai.py / ext_world.py

---

## 11. 附录：P0-2C → P0-2D 的变化

| Event | P0-2C 状态 | P0-2D 冻结 |
|-------|-----------|-----------|
| `message_received` | ✅ 已完成 | ✅ 语义冻结 |
| `message_sent` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `sms_sent` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `relationship_changed` | 🟡 已定位 | ✅ 降为 NONE 级别 |
| `location_changed` | 🟡 15 处 | ✅ **吸收规则冻结**（Section 3） |
| `entered_location` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `work_started` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `work_finished` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `date_invited` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `date_agreed` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `date_started` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `date_finished` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `gift_received` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `item_bought` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `gift_sent` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `instance_created` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `instance_entered` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `instance_paused` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `instance_finished` | 🟡 已定位 | ✅ 语义 + LLM 级别冻结 |
| `activity_started` | ❌ 不存在 | ❌ 已删除 |
| `money_changed` | ❌ 独立 Event | ❌ 不作为 Event |

**总计**：19 个 Event 语义冻结（1 已完成 + 18 新冻结）。

---

## 12. P0-2D 验收状态

**P0-2D = Semantic Freeze only**

### 已完成（语义冻结）

- ✅ Event semantics
- ✅ source / ai_name / payload
- ✅ NONE / LOW / HIGH
- ✅ location_changed boundary
- ✅ Token cost principles
- ✅ Performance boundary
- ✅ Product goal protection

### 尚未实现（架构预留 / 未来 P0-3+ / P1）

- ❌ EventBus
- ❌ Scheduler
- ❌ Wake Score
- ❌ Brain
- ❌ Decision
- ❌ AI-AI autonomous social loop
- ❌ Chat pagination
- ❌ Context retrieval
- ❌ Memory architecture refactor
- ❌ TTS / VoiceStudio

### 因此

**P0-2D 不是"V3.0 已经具备自主 AI 世界"。**

**P0-2D 是：**

> **"把未来自主 AI 世界所需要的 Event 语义边界冻结下来。"**

---

**P0-2D 完成，等待验收。**
```


